export interface DayData {
    poolDayTotalVolume: any   // total volume(in usd) used in day from all pool
    txCountInDay: number   // number of tx happened in day.
}


export class MonthData {
    poolMonthlyTotalVolume: any   // total volume(in usd) used in day from all pool
    txCountInMonth: number   // number of tx happened in day.
}


